server:
  port: 8080

spring:
  application:
    name: blog

  datasource:
    url: jdbc:mysql://127.0.0.1:3306/project
    username: root
    password: root

  redis:
    host: localhost
    port: 6379
    password:

mybatis-plus:
  config-location: classpath:config/mybatis-config.xml
  global-config:
    db-config:
      id-type: auto